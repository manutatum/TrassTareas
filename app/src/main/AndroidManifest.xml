<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Permisos de cámara -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_aplicacion"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_aplicacion_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.ProyectoManuelJimenez"
        tools:targetApi="31">

        <activity
            android:name=".actividades.DescripcionActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.EstadisticasActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.PreferenciasActivity"
            android:exported="false"
            android:label="@string/title_activity_preferencias" />
        <activity
            android:name=".actividades.EditarTareaActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.CrearTareaActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.ListadoTareas"
            android:exported="false" />
        <activity
            android:name=".actividades.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.proyectomanueljimenez.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/rutas" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>